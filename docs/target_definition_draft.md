# Target Definition Draft

Generated by `notebooks/03_target_feasibility.ipynb` at
2026-07-25T09:04:56.975940+00:00.

## 1. Purpose

Record the candidate target definition and its current evidence-based
feasibility status. This is a draft because the modelling scope and final
eligibility rules are not approved.

## 2. Target name

`missed_resolution_target`

## 3. Candidate business definition

`1` means a complaint closed after its expected due date. `0` means it
closed on or before its expected due date.

## 4. Candidate formula

```python
eligible_for_target = (
    created_date.notna()
    & closed_date.notna()
    & due_date.notna()
)
missed_resolution_target = (closed_date > due_date).astype("int8")
```

## 5. Required fields

`unique_key`, `created_date`, `closed_date`, `due_date`, and `status`.

## 6. Eligible population

Historical records containing usable creation, closure, and expected due
timestamps. The target is constructed only inside this population.

## 7. Current DOT dataset scope

Official NYC Open Data dataset `erm2-nwe9`, agency `DOT`, from
`2020-01-01T00:00:00.000` through snapshot maximum
`2026-07-24T01:31:13.000`. The full paginated extract contained
1,469,836 records.

## 8. Current target-field coverage

| column | total_rows | non_null_count | missing_count | coverage_pct | missing_pct |
| --- | --- | --- | --- | --- | --- |
| unique_key | 1469836 | 1469836 | 0 | 100.0 | 0.0 |
| created_date | 1469836 | 1469836 | 0 | 100.0 | 0.0 |
| closed_date | 1469836 | 1437880 | 31956 | 97.82587989408343 | 2.1741201059165784 |
| due_date | 1469836 | 0 | 1469836 | 0.0 | 100.0 |
| status | 1469836 | 1469836 | 0 | 100.0 | 0.0 |

## 8a. Current required-column validation

| column | column_group | present | purpose |
| --- | --- | --- | --- |
| unique_key | target_required | True | Complaint identifier |
| created_date | target_required | True | Creation timestamp and temporal context |
| closed_date | target_required | True | Actual complaint closure timestamp |
| due_date | target_required | True | Expected resolution deadline |
| status | target_required | True | Supports eligibility and lifecycle interpretation |
| agency | analysis_required | True | Required for DOT scope validation and grouped coverage analysis. |
| complaint_type | analysis_required | True | Required for DOT scope validation and grouped coverage analysis. |
| agency_name | optional_descriptive | True | Used only when present for descriptive context or leakage review. |
| resolution_description | optional_descriptive | True | Used only when present for descriptive context or leakage review. |
| resolution_action_updated_date | optional_descriptive | True | Used only when present for descriptive context or leakage review. |

## 9. Current eligible-record count

0 records (0.000000%).

## 10. Open complaint treatment

Open complaints without a closure timestamp cannot receive a historical
closed-on-time versus closed-late label under this definition. They are not
relabelled from status.

## 11. Cancelled and duplicate complaint considerations

Cancelled and duplicate complaints require a documented eligibility rule
before final target creation. Final treatment cannot be approved until a
dataset with usable due dates is selected.

## 12. Timestamp assumptions

Parsed timestamps are timezone-aware UTC values. Source nulls remain null,
parsing failures are reported separately, and no timestamp is imputed.
`due_date` is assumed to represent an expected deadline only provisionally;
its business semantics still require confirmation in a viable source scope.

## 13. Prediction moment

Shortly after complaint creation.

## 14. Label-construction fields

`created_date` defines context and eligibility. `closed_date` supplies the
actual outcome timestamp. `due_date` supplies the expected boundary.

## 15. Leakage exclusions

`closed_date`, final `status`, `resolution_description`, and
`resolution_action_updated_date` must not be creation-time model features.
`due_date` feature use is conditional on proving that it exists at the
prediction moment.

## 16. Invalid substitute targets

| candidate | valid_replacement | reason |
| --- | --- | --- |
| status == "Closed" | No | Measures completion, not whether an expected deadline was missed. |
| closed_date.isna() | No | Mixes unresolved complaints with recently created complaints. |
| Resolution duration above an arbitrary threshold | No | Invents a deadline not supplied by the source. |
| resolution_action_updated_date | No | Is not necessarily the expected resolution deadline. |
| resolution_description | No | Is usually generated after operational action and risks leakage. |
| Missing closed date | No | Does not establish lateness without an expected deadline. |

## 17. Current feasibility decision

**NOT_FEASIBLE** — No records in the current DOT extract contain all timestamps required to construct missed_resolution_target.

Target creation status: **not_created**.

## 18. Primary blocker

No usable due_date values.

## 19. Additional DOT data-quality concern

DOT chronology anomaly. 45,253 DOT records have `closed_date < created_date`, representing 3.147203% of DOT records with both timestamps. The Day 3 full-dataset baseline is 0.212076% (46,492 records). DOT's rate is 14.84x the full-dataset rate and accounts for 97.34% of full-dataset violations. The DOT rate is computed over DOT records with both created_date and closed_date present, while the Day 3 baseline rate is computed over all full-dataset rows rather than the equivalent evaluable-pairs population; the ratio is therefore indicative rather than an exact denominator-matched comparison. This is separate from the zero due-date coverage blocker, and it should inform Day 5's agency and scope comparison. No exclusion rule has been approved for this anomaly in Day 4.

## 20. Known limitations

The NYC Open Data source is live, so future runs can retrieve a different
snapshot and must recompute every count. A non-zero eligible population
would still require due-date semantic validation, class-balance analysis,
status eligibility rules, and prediction-time availability review.

## 21. Required next investigation

Compare target-field coverage across alternative agencies and complaint types.
